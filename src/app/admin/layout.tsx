/**
 * Layout del área admin. Por ahora minimal; en FRONT 3 monta el sidebar
 * navy-900, topbar con avatar/logout, breadcrumbs y forzado de dark mode
 * (filosofía §8 del DESIGN_SYSTEM).
 */
export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
